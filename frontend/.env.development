# 开发环境不显式指定 API_URL，由 api.ts 回退到 window.location.origin，
# 这样无论是 localhost 还是内网 IP（如 http://192.168.31.136:5174/）访问，
# 前端请求都会走当前页面地址，再由 Vite 代理转发到后端。
#
# 如需让浏览器直连后端（绕过 Vite 代理），可在此设置：
#   VITE_API_URL=http://localhost:3333
# 但注意：使用内网 IP 访问时此配置会导致请求仍指向 localhost，无法连上。
#
# 如需指定后端地址（Vite 代理目标），请在启动前设置 VITE_API_TARGET 环境变量：
#   $env:VITE_API_TARGET="http://127.0.0.1:3333"; npm run dev
