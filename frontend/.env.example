# 前端 Socket.IO / API 基础地址
# 本地开发建议使用 "/"，通过 Vite 代理转发到后端；
# 若遇到 /api/stream/resolve-bilibili 等 NDJSON 流式接口被异常中断，
# 可在 .env.development 中设置为 http://localhost:3333 直接访问后端。
# 生产环境留空或不设置时，默认使用当前页面域名（统一端口后由后端托管前端静态文件）。
VITE_API_URL=/

# OBS 推流模式 HTTP-FLV 拉流基础地址（可选）。
# 统一端口后保持留空即可，前端默认使用相对路径 /live，由后端反向代理到 NMS 的 HTTP-FLV 端口（默认 3335）。
# 若使用独立子域名（如 https://flv.example.com）或不同路径，请显式设置：
# VITE_FLV_BASE_URL=https://your-domain.com/live
# VITE_FLV_BASE_URL=
